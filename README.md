This pipeline processes raw reads from BRB_Seq RNA-sequencing to a finished count matrix. This pipeline is SOP for the Muegge Lab.

The initial goal is to move this from bash/SLURM scripting to a python environment. This will also pull run specific parameters into a yaml file.