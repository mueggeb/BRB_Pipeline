# mamba env create -f environment.yml -p /ref/bmlab/software/envs/brb_pipeline
name: brb_pipeline
prefix: /ref/bmlab/software/envs/brb_pipeline
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - cutadapt
  - fastqc
  - star
  - subread
  - rseqc
  - samtools
  - multiqc
  - pandas
